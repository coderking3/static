# Prompt 指南 · 阶段三：质量保障

> 代码写完了，要保证没问题。需要 AI 帮你审查、补测试、出文档、配部署。

---

## 场景一：代码审查

> 代码写完了，想让 AI 帮看一眼有没有问题。

**适用**：PR review、上线前检查、自查。

```markdown
# Review：[模块 / PR]

## 改动内容

[这次改了什么、为什么]

## 代码

[贴代码 或 指定文件路径]

## 请你审查

- 有没有逻辑 bug
- 有没有安全风险
- 有没有性能问题
- 命名和结构是否合理
- 有没有遗漏的边界情况

## 不需要关注

- [代码风格 / 格式化等]
```

---

## 场景二：测试编写

> 给已有代码补测试，或写 E2E 测试。

**适用**：补单测、写集成测试、E2E 测试。

```markdown
# 测试：[模块名称]

## 被测代码

[文件路径 或 贴代码]

## 测试范围

- [ ] 正常流程
- [ ] 边界值
- [ ] 异常 / 错误处理
- [ ] [其他需要覆盖的场景]

## 测试框架

[现有测试框架，如 jest / vitest / pytest]

## 要求

- 参考现有测试风格：[现有测试文件路径]
- mock 外部依赖，不要连真实服务
- 测试命名要能表达意图
```

---

## 场景三：文档生成

> 根据代码或接口生成文档。

**适用**：API 文档、README、使用说明。

```markdown
# 文档：[文档类型]

## 源

[代码路径 或 接口定义]

## 受众

[前端 / 后端 / 产品 / 新人]

## 要求

- 格式：[Markdown / OpenAPI / 其他]
- 包含：[安装 / 使用示例 / 参数说明 / 注意事项]
- 参考：[现有文档链接]

## 约束

- 不要照搬代码注释，要面向读者重新组织
- 给出可直接复制的示例代码
```

---

## 场景四：部署运维

> CI/CD 配置、Docker、发布脚本等基础设施。

**适用**：新建流水线、Docker 化、发布脚本、环境配置。

```markdown
# 运维：[目标]

## 项目信息

- 语言 / 框架：[xxx]
- 构建命令：[npm run build / mvn package / ...]
- 产物路径：[dist / target / ...]
- 运行环境：[Node 16 / JDK 11 / ...]

## 目标

[要实现什么，如：配置 CI/CD、Docker 化、自动部署]

## 现有配置

- [现有 CI 配置文件路径，如有]
- [现有 Dockerfile，如有]

## 约束

- 参考现有项目的配置风格
- [环境要求：内网 / 特定镜像源 / ...]
```

---

## 本阶段原则

- **明确关注点**：审查时说清"查什么"和"不查什么"，AI 不会自己判断
- **给标准**：测试要覆盖什么场景、文档面向谁写，都要明说
- **给参考**：指定"参考现有 xxx 的风格"，比让 AI 从零发挥可靠得多
- **审查要带上下文**：告诉 AI 为什么改这段代码，它才能判断改得对不对
